# tmux alias
# シェル起動時に毎回読み込む

# Tmux
alias tm='tmux'
alias tma='tmux attach'
alias tml='tmux ls'
alias tmk='tmux kill-session -t'

# Tmuxinator
alias tmi='tmuxinator'
alias tmis='tmuxinator start'

# Edit
alias tmedit='vim ~/.config/tmux/tmux.conf'
